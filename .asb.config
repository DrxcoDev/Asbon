!!Vwesion 0.1

USERREPOSITORY {
    * (src/**),
    build (
        !FUNCTIONS(**.c, **.h);
    ) = output(.exe) ;windows
    SRC::init{
        if(_diry = {compile.c, compile.h, lib.h, main.h, main.dll}){
            use::GCC(mainloop)
        }
        else::notesd
        #enfre
    }
}
